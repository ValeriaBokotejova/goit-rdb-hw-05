# goit-rdb-hw-05

## 🧩 SQL Subqueries and Functions

This project includes:

- Subqueries in `SELECT`, `WHERE`, and `FROM` clauses
- Common Table Expression (CTE) using `WITH`
- Custom SQL function with two `FLOAT` parameters

All queries were executed and tested in MySQL Workbench.
